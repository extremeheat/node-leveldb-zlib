{
  "name": "leveldb-zlib",
  "version": "1.0.0",
  "description": "Node.js bindings for leveldb with zlib",
  "main": "index.js",
  "scripts": {
    "install": "cmake-js compile",
    "build": "tsc",
    "test": "npm run build && cd tests && ts-node iterate.ts"
  },
  "author": "extremeheat",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.16",
    "cmake-js": "^6.1.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "napi-macros": "^2.0.0",
    "node-addon-api": "^3.1.0",
    "segfault-handler": "^1.3.0"
  }
}
